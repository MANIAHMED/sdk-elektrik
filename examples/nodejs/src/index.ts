// import { OrderBookApi, SupportedChainId } from 'pro-analytics-sdk'

// See more examples in /examples/cra
// ;(async function () {
//   const orderBookApi = new OrderBookApi({ chainId: SupportedChainId.LIGHTLINK_PHOENIX_MAINNET })

//   const order = await orderBookApi.getVersion()

// })()
